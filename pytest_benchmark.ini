[pytest]
testpaths = tests
python_files = test_benchmarks.py
python_functions = test_*

addopts = 
    --verbose
    --benchmark-only

markers =
    benchmark: marks tests as benchmarks

filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning 